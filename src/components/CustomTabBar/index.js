import {View, Text} from 'react-native';
import {TabArea, TabItem, TabItemCenter} from './styles';
import db from '../../../db.json';

export default () => {
  return (
    <TabArea>
      <Text>Olá tab bar</Text>
    </TabArea>
  );
};
